# ─── Application ──────────────────────────────────────────────────────────────
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here

# ─── Database ─────────────────────────────────────────────────────────────────
# SQLite (default for local dev)
DATABASE_URL="file:./dev.db"
# PostgreSQL (for production — uncomment and set):
# DATABASE_URL="postgresql://user:password@host:5432/news_dashboard?schema=public"

# ─── Admin Seed User ──────────────────────────────────────────────────────────
# Used by: npm run db:seed
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=changeme123!

# ─── News API (Adapter B — optional) ─────────────────────────────────────────
# Sign up at https://newsapi.org or https://newsdata.io
# Leave empty to disable the News API adapter
NEWS_API_KEY=
NEWS_API_PROVIDER=newsapi   # newsapi | newsdata

# ─── Ingestion Schedule ───────────────────────────────────────────────────────
# Interval in minutes for the background ingestion job (local dev polling)
INGEST_INTERVAL_MINUTES=15

# Cron secret for securing the /api/cron endpoint (Vercel Cron or external cron)
# Generate with: openssl rand -hex 32
CRON_SECRET=your-cron-secret-here

# ─── Rate Limiting ────────────────────────────────────────────────────────────
# Max ingestion triggers per hour from the same IP
INGEST_RATE_LIMIT=10

# ─── Logo & Branding (Designer TODO) ─────────────────────────────────────────
# See: src/lib/tokens.ts and DESIGNER_NOTES.md for instructions on
# replacing the placeholder theme with your logo's color palette.
NEXT_PUBLIC_SITE_NAME="News Dashboard"
NEXT_PUBLIC_LOGO_PATH=   # e.g. /logo.svg — place file in /public
